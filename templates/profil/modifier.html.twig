{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Modif profil{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('accueil') }}" class="lien">Accueil</a></li>
            <li class="breadcrumb-item"><a href="{{ path('user_profil',{id:user.id}) }}" class="lien">Détails profil</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">Modification profil</li>
        </ol>
    </nav>
    <h1 class="container row justify-content-center">Modifies ton profil, {{ app.user.pseudo }}</h1>
    <section id="modifier-profil">


        {{ form_start (formUser) }}
            <div>
            {{ form_label(formUser.pseudo) }}
            </div>
             <div class="col-2">
            {{ form_widget(formUser.pseudo) }}
            </div>
            <div class="col-2">
            {{ form_label(formUser.email) }}
            </div>
            <div class="col-2">
            {{ form_widget(formUser.email) }}
            </div>
            <div class="col-2">
            {{ form_label(formUser.telephone) }}
            </div>
            <div class="col-2">
                {{ form_widget(formUser.telephone) }}
            </div>
            <div class="col-2">
                {{ form_label(formUser.plainPassword) }}
            </div>
            <div class="col-2">
                {{ form_widget(formUser.plainPassword) }}
            </div>
            <div class="col-2">
                {{ form_label(formUser.prenom) }}
            </div>
            <div class="col-2">
                {{ form_widget(formUser.prenom) }}
            </div>
            <div class="col-2">
                {{ form_label(formUser.nom) }}
            </div>
            <div class="col-2">
                {{ form_widget(formUser.nom) }}
            </div>
            <div class="col-2">
                {{ form_label(formUser.new_password) }}
            </div>
            <div class="col-2">
                {{ form_widget(formUser.new_password) }}
            </div>

            <div class="form-group ">
                <button type="submit" class="btn btn-page">Enregistrer</button>
                <a href="{{ path('user_profil',{id:user.id}) }}" class="btn btn-secondary">Retour</a>
            </div>
            {{ form_end (formUser) }}



    </section>


{% endblock %}
